# 14. План работ: хакатон (48 ч) и после

Команда-референс: 4 человека — B1 (Go, инфраструктура/конвейер), B2 (Go, retrieval/answer), M (Python, parse/extract/eval), F (frontend + продукт/питч). При другом составе — переразметка ролей, критический путь не меняется.

## 0. До старта (сделано/делается до 03.07)

Тайминг организаторов (чат капитанов, 02.07): полные описания задач — **02.07 22:00**; доступы (датасет, реестр моделей) — **утром 03.07**; работать над задачей можно с момента публикации описания → ночь 02→03.07 используем для скелета и numcore на демо-корпусе-заглушке, утром переключаемся на реальные данные и реестр моделей.

- [x] Анализ ТЗ и RFC, гипотезы, выбор решения (этот пакет документов).
- [ ] Скелет монорепо: contracts (proto+events), pkg (config/otel/outbox/events), каркасы сервисов, compose, Makefile, CI.
- [ ] Seeds: единицы (~90), parameter_defs (~40), словарь синонимов из ТЗ, географии, домены.
- [ ] numcore v1 + golden-тесты на текстах из ТЗ (примеры Q1–Q6 уже дают паттерны).
- [ ] Демо-корпус-заглушка (20 открытых статей/патентов RU/EN по электроэкстракции/обессоливанию) — заменится датасетом хакатона.
- [ ] Запросить ресурсы ([15-resources.md](15-resources.md)): LLM API endpoint, GPU, доступ к датасету.

## 1. День 1 (данные и хребет)

| Часы | B1 | B2 | M | F |
|---|---|---|---|---|
| 0–4 | compose up: PG18+pgvector, NATS, MinIO; миграции+seeds; ingest (upload, sha256, outbox) | kmap-llm (upstream конфиг, схемы, кэш) + kmap-embed интеграция | kmap-parse (Docling→DocIR) на датасете хакатона; разбор форматов каталога экспериментов | каркас UI: layout, search bar, SSE-клиент, таблица evidence |
| 4–8 | catalog: entity resolution + коммит фактов + edges | search: SQL-канал (факты, numrange) + вектор + FTS + RRF | extract: чанкинг+эмбеддинги+numcore на реальных данных; калибровка suspect'ов | вкладки ответа, карточка факта с цитатой и score-компонентами |
| 8–12 | конвейер целиком на 50 документах; статусы, DLQ | answer: QueryPlan (rules-контур) → search → экстрактивный ответ | LLM-extraction (entities/relations) + bind_numbers; bundle.quality | фильтры, пресеты Q1–Q6 |
| контрольная точка КТ-1 (12 ч) | **Q1 и Q3 отвечают экстрактивно с provenance на реальном датасете** | | | |

## 2. День 2 (дифференциаторы и полировка)

| Часы | B1 | B2 | M | F |
|---|---|---|---|---|
| 12–18 | epistemic: кластеры+консенсус; coverage-ячейки | answer: LLM-парсер плана + синтез + **numeric guard** + SSE | gold-set v1 из датасета (16+ вопросов), eval-harness, первый прогон | вкладки Противоречия/Пробелы/Эксперты; ego-граф (Cytoscape) |
| 18–24 | contradictions: кандидаты + judge + статусы; expert_topics | comparison-декомпозиция (Q4: RU vs мир, таблица) | прогоны eval, тюнинг ранжирования/промптов по метрикам | heatmap руководителя; плашка guard; экспорт MD/CSV |
| 24–30 | стабилизация конвейера, полный переиндекс; **RBAC middleware + RLS-политики + `auth.mode: demo`** (роли/уровни доступа — требование ТЗ) | кэш ответов, латентность до SLO | A/B моделей (20b/120b/flash) — таблица «качество vs токены» | Entity Page, Experiment Explorer (минимум); переключатель demo-ролей |
| 30–36 | прогон 6 демо-сценариев ×10, фиксация датасета | багфиксы горячего пути | финальный eval-отчёт (цифры для слайдов) | лоск UI, скриншоты, запись резервного видео |
| КТ-2 (24 ч): Q1–Q6 работают end-to-end. КТ-3 (36 ч): метрики сняты, демо отрепетировано | | | | |

## 3. Финал (36–48 ч)

- Freeze кода и датасета; README quickstart (`task demo`); резервный сценарий без live-ingestion.
- Питч (5–7 мин): боль → «дни→минуты» → live Q1 (числа+provenance+guard) → Q2 (противоречие с причиной) → Q5 (heatmap+эксперты) → метрики (слайд из [13-evaluation.md](13-evaluation.md) §5) → архитектура одним слайдом (прод-готовность: Go, события, идемпотентность, деградации) → экономика ресурсов (таблица моделей).
- Q&A-подготовка: «чем не plain RAG» (guard, структурные фильтры, judge, heatmap — показать вживую), «почему без Neo4j» (E4/E6-замеры, пороги пересмотра), «как масштабируется» ([11-nfr.md](11-nfr.md)).

## 4. Деградация скоупа (если горим)

Порядок отбрасывания (обратный ценности): PDF-экспорт → Entity Page/Explorer (остаются API) → heatmap-интерактив (статический скрин из реальных данных) → LLM-синтез (полностью экстрактивные ответы — система остаётся честной) → live-ingestion в демо (только пре-индекс). **Не отбрасываются никогда:** numcore+guard, структурные фильтры, provenance, evidence-таблица, противоречие Q2, эксперты Q6.

## 5. После хакатона (прод-фазы)

- **Phase 1 (пилот, 1–2 мес):** hardening прод-VM (TLS/caddy, ufw, офф-VM бэкапы + restore-репетиции, алерты on-call), Keycloak с корп. IdP, RLS-политики на реальные грейды доступа, полный аудит, нагрузочные k6; онбординг реального корпуса; экспертная валидация (очередь ревью) с 2–3 экспертами заказчика; калибровка condition_class bands доменными специалистами.
- **Phase 2:** kmap-notify (подписки/дайджесты), PDF-экспорт (gotenberg), JSON-LD полный, ручная правка графа в UI, sparse-гибрид bge-m3, инкрементальный крауд-словарь синонимов.
- **Phase 3:** асинхронный deep-research отчёт (наследие E2 — вне SLA интерактива), новые домены (РЗЭ), коннекторы к корпоративным DMS/LIMS, датчики установок (стриминг в отдельный контур), рекомендательные подсказки «смежные кейсы» на ко-эмбеддингах ячеек coverage; масштабирование за пределы одной VM по триггерам [12-deployment.md](12-deployment.md) §7 (вынос PG → воркер-VM → k8s при необходимости).
